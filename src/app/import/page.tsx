import { AppNav } from "@/components/app-nav";

export default function ImportPage() {
  return (
    <main>
      <AppNav />
      <section className="glass-card" style={{ padding: 24 }}>
        <h1 style={{ marginTop: 0, marginBottom: 8 }}>Import</h1>
        <p className="meta" style={{ margin: 0 }}>
          Import (Coming Soon)
        </p>
      </section>
    </main>
  );
}
